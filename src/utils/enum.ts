export enum RoutePath {
  HOME = '/',
  LOGIN = 'login',
  SIGN_UP = 'sign-up',
  EDITOR = '/editor',
  NOT_FOUND = '*',
}

export enum LocalStorageKeys {
  LANGUAGE = 'lang',
  THEME = 'theme',
}

export enum Docs {
  ROOT_TYPES = 'Root Types',
  FIELDS = 'Fields',
  TYPE = 'Type',
  ARGS = 'Arguments',
}
